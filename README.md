# json_schema_validation_sample

This contains a sample json schema using the jsonschema draft07 format and a sample json data. This will not have any error. 
To play around with the validator, change the values of the properties on the 'user.json' file and run the json_validate.py
